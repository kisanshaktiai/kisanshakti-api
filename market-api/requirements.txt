beautifulsoup4==4.12.2
requests==2.31.0
lxml==4.9.3

# Supabase python client (v2+). pinned to a modern, available release to avoid old gotrue pins
supabase==2.25.0

# Auth library (modern replacement for old 'gotrue' usage). Include if you use auth-specific features.
supabase-auth==2.25.0

# Postgrest client (used by supabase runtime)
postgrest==2.25.0
